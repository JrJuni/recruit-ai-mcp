# LLM — 둘 중 하나 선택 (chatgpt_oauth는 CLI 로그인 후 토큰 자동 관리, 키 불필요)
ANTHROPIC_API_KEY=sk-ant-...

# MongoDB Atlas — M0 free tier URI
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority
